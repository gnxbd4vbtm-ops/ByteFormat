# ByteFormat VS Code Extension

Provides syntax highlighting and a custom file icon for ByteFormat `.byte` files.

What's included
- Syntax grammar tuned to use JSON-like scopes so strings, numbers etc.